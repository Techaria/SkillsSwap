<!DOCTYPE html>
<html lang="en">
<head>
  
  <meta charset="UTF-8">
  <title>Request Help</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-blue-50 to-white min-h-screen flex flex-col">
  
  <!-- Header -->
  <header class="bg-white shadow-md p-5 flex justify-between items-center">
    <h1 class="text-2xl font-bold text-blue-700">Request a Skill</h1>
    <%- include('partials/navbar') %>
  </header>

  <!-- Main Content -->
  <main class="flex-grow flex items-center justify-center p-6">
    <div class="bg-white p-10 rounded-3xl shadow-2xl w-full max-w-2xl border border-blue-100">
      <h2 class="text-3xl font-semibold text-gray-800 mb-3">Need Some Help?</h2>
      <p class="text-gray-500 mb-6">Submit a request and let others offer their expertise.</p>

      <form action="/request" method="POST" class="space-y-6">
        <!-- Title -->
        <div>
          <label class="block text-gray-700 font-medium mb-1">Request Title</label>
          <input type="text" name="title" placeholder="e.g., Need help with website design" class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:ring-2 focus:ring-indigo-400 focus:outline-none" required>
        </div>

        <!-- Description -->
        <div>
          <label class="block text-gray-700 font-medium mb-1">Description</label>
          <textarea name="description" rows="4" placeholder="Describe your need in detail..." class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:ring-2 focus:ring-indigo-400 focus:outline-none" required></textarea>
        </div>

        <!-- Tokens Offered -->
        <div>
          <label class="block text-gray-700 font-medium mb-1">Tokens You're Offering</label>
          <input type="number" name="token_value" min="1" class="w-full border border-gray-300 rounded-xl px-4 py-2 focus:ring-2 focus:ring-indigo-400 focus:outline-none" required>
        </div>

        <!-- Submit -->
        <div class="pt-4">
          <button type="submit" class="w-full bg-indigo-600 text-white font-semibold py-3 px-6 rounded-xl hover:bg-indigo-700 transition duration-200">
            Submit Request
          </button>
        </div>
      </form>
    </div>
  </main>

</body>
</html>
